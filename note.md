## 用户系统

- 注册:
通过密码和用户名进行注册账户, 注册将生成一个唯一用户id(UID)进行用户索引和标识;

- 鉴权:
通过 UID 密码 用户名 生成一个Token来存储, 当用户登入时输入用户名(或UID)和密码传入Token生成对存储的进行比对, 如果一致则鉴权通过;

- 临时登入:
通过一个UID对应用户的Session和生成时间, Session由 时间戳 Token 来生成, 用户携带Session信息, 每次进行操作时携带该信息由服务端进行Session比对, 若超时或无效则操作和用户登入状态无效, 若比对成功则认定为登入状态并进行刷新Session;
